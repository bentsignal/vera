{
  "name": "vera",
  "private": true,
  "engines": {
    "node": ">=24 <25",
    "pnpm": "10.32.1"
  },
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules .turbo",
    "clean:all": "turbo run clean && pnpm run clean",
    "dev": "portless proxy start && turbo run dev --parallel --continue",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "turbo run format --continue -- --write --cache --cache-location .cache/.prettiercache",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "lint:ws": "pnpm dlx sherif@latest",
    "postinstall": "pnpm lint:ws",
    "typecheck": "turbo run typecheck",
    "check": "pnpm run lint && pnpm run typecheck",
    "ui-add": "turbo run ui-add",
    "create-package": "turbo gen init",
    "loc": "cloc --exclude-dir=_generated --not-match-f=routeTree.gen.ts apps/*/src packages/*/src"
  },
  "devDependencies": {
    "@acme/prettier-config": "workspace:*",
    "@effect/language-service": "0.85.1",
    "@turbo/gen": "2.8.17",
    "@typescript/native-preview": "catalog:",
    "dotenv-cli": "10.0.0",
    "portless": "^0.12.0",
    "prettier": "catalog:",
    "turbo": "2.8.17",
    "typescript": "catalog:"
  },
  "prettier": "@acme/prettier-config"
}
