{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".tanstack/**", ".output/**", ".vercel/output/**"]
    },
    "dev": {
      "persistent": true
    }
  }
}
